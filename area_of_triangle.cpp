#include<iostream>
using namespace std;
int main()
{
    float h,b,area;
    cout<<"enter base(b) and height(h):\n";
    cin>>b>>h;
    area=(b*h)/2;
    cout<<"area of triangle is -->"<<area;
    return 0;
}